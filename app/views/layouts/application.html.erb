<!DOCTYPE html>
<!-- paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither/ -->
<!--[if lt IE 7 ]> <html class="ie6" lang="en"> <![endif]-->
<!--[if IE 7 ]>    <html class="ie7" lang="en"> <![endif]-->
<!--[if IE 8 ]>    <html class="ie8" lang="en"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--> <html lang="en"> <!--<![endif]-->
  <head>
  	<meta charset="utf-8" />

  	<!-- Uncomment to make IE8 render like IE7 -->
  	<!-- <meta http-equiv="X-UA-Compatible" content="IE=7" /> -->

  	<!-- Set the viewport width to device width for mobile -->
  	<meta name="viewport" content="width=device-width, initial-scale=1.0" />

  	<title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

  	<%= stylesheet_link_tag    "application" %>
  	<%= javascript_include_tag "vendor/custom.modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="top-bar">
      <ul class="title-area">
	<li class="name">
	  <h1><%= link_to 'ИвГМА', 'http://isma.ivanovo.ru' %></h1>
	</li>
      </ul>

      <section class="top-bar-section">
	<ul class="left">
	  <li class="divider"></li>
	  <li><%= link_to t(:fresh_publication), issue_publication_path(@last_publication.issue, @last_publication) %>
	<% @menus.each do |menu| %>
	  <li class="divider"></li>
	  <li><%= link_to t(menu.title), menu.path %>
	<% end %>
	</ul>
	<ul class="right">
    <li><%= link_to params[:locale] == 'ru' ? 'En' : 'Рус', '#' %>
	  <li class="divider"></li>
	  <li><%= @current_user ? link_to("Выйти", logout_path) : link_to("Войти", login_path) %></li>
	</ul>
      </section>
    </nav>

    <div class="row">
      <div class="small-12 large-12 columns"><h1>Вестник ИвГМА</h1></div>
    </div>
    <div class="row">
      <div class="small-3 large-2 columns">Новости</div>
      <div class="small-9 large-10 columns"><%= yield %></div>
    </div>
    

    
    <%= javascript_include_tag "application" %>
  </body>
</html>
